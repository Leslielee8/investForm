import { InputValidaterDirective } from './input-validater.directive';

describe('InputValidaterDirective', () => {
  it('should create an instance', () => {
    const directive = new InputValidaterDirective();
    expect(directive).toBeTruthy();
  });
});
